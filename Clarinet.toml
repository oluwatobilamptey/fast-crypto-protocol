[project]
name = "fast-crypto-protocol"
authors = ["Fast Protocol Team"]
description = "An achievement tracking protocol for decentralized goal verification"
telemetry = false
requirements = []
[contracts.achievement-tracker]
path = "contracts/achievement-tracker.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
